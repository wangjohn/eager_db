source "https://rubygems.org"

gem 'activesupport'
gem 'resque', '~> 1.25'
gem 'minitest'
gem 'activerecord'
gem 'rake'

# To get this to run with the eager_db gem locally, you need to the two things:
#  1. cd into your eager_db git directory.
#  2. Run `gem build eager_db.gemspec`, this should create a .gem file in your
#     directory.
#  3. Set the gem path (below) to the correct location.
gem 'eager_db', path: '~/eager_db'
